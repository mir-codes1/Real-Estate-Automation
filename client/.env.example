# ─── Dev proxy target ─────────────────────────────────────────────────────────
# URL of the Express backend. Used by vite.config.js to proxy /api/* requests
# during development. Not bundled into the production build.
BACKEND_URL=http://localhost:3001
